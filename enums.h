#ifndef __INCLUDE_ENUMS_H__
#define __INCLUDE_ENUMS_H__

typedef enum order_direction {
	ORDER_DIR_UP   = 0,
    ORDER_DIR_DOWN = 1,
    ORDER_DIR_BOTH = 2
} order_direction;

#endif // ifndef __INCLUDE_ENUMS_H__